<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background -->
    <solid android:color="#FFFFFF"/>

    <!-- Rounded Corners -->
    <corners android:radius="18dp"/>

    <!-- Soft Border -->
    <stroke
        android:width="1.5dp"
        android:color="#C7DAFF"/>

    <!-- Inner Padding -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="10dp"
        android:bottom="10dp"/>

</shape>